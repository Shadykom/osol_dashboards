import React from 'react';

// Placeholder calendar component.  In a real project you would import a
// proper date range picker.  Here we simply render its children.

export const Calendar = ({ mode, selected, onSelect, numberOfMonths }) => {
  return <div>Calendar component placeholder</div>;
};

export default Calendar;